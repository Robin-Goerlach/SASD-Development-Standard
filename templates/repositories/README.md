# Repository Templates

The `dotnet/` directory contains starting points for SASD C#/.NET repositories. Templates must be adapted to the selected SDK, quality level and deployment model.

## .NET Desktop

The `dotnet/desktop/` directory contains baseline project files for WinForms and WPF. Select a supported target framework and review high-DPI, packaging and deployment settings before use.
