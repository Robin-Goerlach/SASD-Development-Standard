# Small .NET Tool

```text
src/Sasd.SampleTool/
tests/Sasd.SampleTool.Tests/
```

Use when the tool has one deployment unit and limited integration complexity. Keep responsibilities visible through feature-oriented folders. Do not create empty layers.
